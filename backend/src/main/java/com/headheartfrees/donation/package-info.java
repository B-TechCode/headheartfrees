/**
 * Donation intents.
 *
 * <p>Module boundary (PROJECT_BRIEF.md section 4): a donation intent references
 * its optional donor by {@code UUID} user id, never by an entity imported from
 * {@code auth}.
 *
 * <p>Implemented in phase 8. Empty by design until then.
 */
package com.headheartfrees.donation;
